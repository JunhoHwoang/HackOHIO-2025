import {create} from 'zustand';type Filters={permit?:string;payment?:string;showADA?:boolean};type AppState={userLocation?:{lat:number;lng:number};setUserLocation:(p:{lat:number;lng:number})=>void;filters:Filters;setFilters:(f:Partial<Filters>)=>void};export const useApp=create<AppState>((set)=>({filters:{},setFilters:(f)=>set((s)=>({filters:{...s.filters,...f}})),setUserLocation:(p)=>set({userLocation:p})}));