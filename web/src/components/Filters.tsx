import { useApp } from '../state/store'
export default function Filters(){const filters=useApp(s=>s.filters);const setFilters=useApp(s=>s.setFilters);return(<div className='p-3 border-b flex items-center gap-3'><div className='text-sm text-gray-700'>Filters:</div><select className='border rounded px-2 py-1 text-sm' value={filters.permit||''} onChange={e=> setFilters({permit: e.target.value || undefined})}><option value=''>Any Permit</option><option value='A'>A</option><option value='B'>B</option><option value='C'>C</option><option value='Visitor'>Visitor</option></select></div>) }